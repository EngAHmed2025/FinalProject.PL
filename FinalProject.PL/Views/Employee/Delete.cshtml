@model EmployeeViewModel
@{
    ViewData["Title"] = "Delete";
}

<h1>Are you sure do you want Delete this Employee!?</h1>
<br />
<br/>
<form asp-action="Delete"asp-route-id="@Model.Id">
    <partial name="PartialViews/DeleteDetailsEmployeePartial"></partial>
    <input type="hidden" asp-for="ImageName" />
    <input type="submit"  value="Delete" class="btn btn-danger mt-5 "/>
    <a asp-action="Index" class="btn btn-light mt-5 ">Cancel</a>
</form>